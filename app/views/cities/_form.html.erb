<%= simple_form_for(@city, :validate => true) do |f| %>
  <%#= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
     <%= link_to "Back",cities_path, class: 'btn btn-default'  %>
  </div>
<% end %>
